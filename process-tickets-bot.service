# Файл автозапуска для systemd.
# Установка (пути поменяйте, если бот лежит не в /opt/PROCESS-Tickets-bot):
#   cp process-tickets-bot.service /etc/systemd/system/
#   systemctl daemon-reload
#   systemctl enable --now process-tickets-bot

[Unit]
Description=Telegram-бот отчётов о билетах (QTickets)
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=/opt/PROCESS-Tickets-bot
ExecStart=/opt/PROCESS-Tickets-bot/venv/bin/python -u main.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
